body {
  font-family: Arial, sans-serif;
  background: #f0f8ff;
  margin: 0;
  padding: 0;
}

header {
  background: #004080;
  color: white;
  text-align: center;
  padding: 1em;
}

main {
  padding: 2em;
}

section {
  background: white;
  border: 1px solid #ccc;
  padding: 1em;
  margin-bottom: 2em;
  border-radius: 8px;
}

label {
  display: block;
  margin-bottom: 1em;
}

button {
  background: #004080;
  color: white;
  border: none;
  padding: 0.5em 1em;
  border-radius: 4px;
  cursor: pointer;
}

footer {
  text-align: center;
  font-size: 0.8em;
  padding: 1em;
  background: #e0e0e0;
}
